@client.event
async def on_message(message):
  if str(message.channel) == "no-text" and message.content !="":
    await message.channel.purge(limit=1)


#

#send message at a certain time
from discord.ext import tasks

client = commands.Bot(command_prefix="!")

'@'tasks.loop(hours=1.0) #without quotation marks Reddit won't let me use the at sign without them

async def send_message()

channel = client.get_channel(channel id goes here)

channel.send("Message")



'@'client.event

async def on_ready()

send_message.start()